# Social Network Server
Rest API for a Social Network written with express. Includes authentication with passportjs and session storage in Redis.

## Requirement
You must have npm and node installed on your system. Also, you must create a .env file and declare the variables shown in the .env.example file

## Usage
```
npm install
npm start
```
